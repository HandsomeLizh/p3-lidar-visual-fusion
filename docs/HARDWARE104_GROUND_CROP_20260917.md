# 104：点云更新但高程栅格不增长的修复

2026-09-17，代码修复提交 `37c9a03`。已经在 104 编译并重启建图，237 专用窗口也已重开。

## 原因与修改

旧顺序是车身过滤、相机视野裁剪、近地面拟合、栅格融合。现场转向后的连续 8 帧记录中，裁剪后只剩 18～24 个近地面候选点，达不到原有 50 点要求，整帧高程输入被拒绝；三维点云存储仍在更新。237 接收的地图消息正常，内容没有新增高程。

现在用同一帧经过车身过滤的完整 LiDAR 扫描提供地面拟合依据，再筛选相机视野内的实测点写入地图。视野外点仅用于估计地面，不写入这张点云或栅格图。原地面可靠性、车身、高处回波及位姿不确定性条件保留。

## 已验证

- 地面筛选及车身过滤的 7 项单元测试通过。
- 同一批 8 帧真实点云和位姿，在相同固定测试协方差下：旧版高程 0 格，新版 603 格；两次保存的三维点云均为 3741 点且逐点一致。这是几何处理回归，不代表行驶定位精度。
- 上述单帧建图处理中位耗时：122 → 158 毫秒；95 分位：158 → 170 毫秒。
- 新运行目录：`results/run_20260917_155323`。实时地面筛选一帧接纳约 7594 点；高程随后仍按实测位姿不确定性逐格判断。
- 237 的 20 秒接收检查：局部图约 1.03 Hz，全局图约 0.51 Hz；二者均收到 579 个有效高程格，窗口已检查。
- ROS 域仍为 59；外发局部图 64×64 米、0.2 米一格；右侧默认跟车视野 32×32 米。双目仅辅助定位，三维补点关闭。

重启已经创建新地图原点，P4 需要重新对齐。旧运行及对照记录保留在 `results/run_20260917_152419` 和 `results/ground_crop_regression_20260917_v2`。

本次修复不证明长时间行驶或失配恢复已经验证；未发送车辆行驶或目标点指令。现场还观察到手动面板仍开着、底盘驱动已退出，其日志最后更新在 15:51 左右，退出原因未确认。
