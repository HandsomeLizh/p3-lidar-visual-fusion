# 104 栅格更新慢：2026-09-17 核查与修复

## 结论

本次问题主要不是点数不足。一帧约 7800 个地形点可以形成约 520 个格子，
但曾只有 3 个格子进入高程融合。需要区分三件事：

1. 已修复相机视野裁剪后找不到地面支撑的问题，见 `HARDWARE104_GROUND_CROP_20260917.md`。
2. 已修复融合器输出时间抢先于延迟雷达测量的问题。
3. 已修复 IMU 与大点云共用 Python 接收进程时丢失中间样本的问题。

代码已编译、同步，并在用户确认停稳后完成切换。当前运行目录为
`results/run_20260917_165024`，104 建图和 237 专用显示窗口均已重启。
本轮已验证 IMU 连续接收、雷达正常配准、栅格持续写入及跨机显示。

## 证据

| 检查 | 结果 | 适用范围 |
|---|---|---|
| 真实点云与完整位姿协方差 | 点本身高度离散约 3–4 cm，位姿投影后的高度标准差明显超出 25 cm 门限 | 解释格子被拒绝，不能当作实际测量误差 |
| 实际 ROS EKF 延迟输入回归 | 旧流程测试格子通过率 0%，新流程 100% | 人工构造时序，不是实车精度测试 |
| 20 秒原始／转发 IMU 对照 | 原始 100 Hz，旧转发约 86 Hz；转发最大时间缺口 270 ms | 同机、同期被动采样 |
| 拆分接收的同期对照 | 原始和新转发均 100 Hz；新转发没有超过 40 ms 的缺口；旧转发约 68 Hz、49 次缺口 | 两条链同时接收，测试输出独立 ROS 域 98 |
| 237 接收检查 | 能收到局部、全局图；当时均为 703 个已知高程格 | 只证明传输，不证明地图增长 |
| 最终版本首轮 45 秒检查 | 局部已知高程格 518 → 1496，全局约 499 → 1495；237 确认收到 1495 格 | 实际在线数据，包含启动和切换过程 |
| 最终版本后续 20 秒检查 | 抽查 11 次地图更新，接纳 6283 个格子观测、拒绝 0；局部已知格 1507 → 1565；收到 2004 个 IMU 样本 | 格子观测包含重复观测，不是新增格数量 |
| 雷达持续跟踪 | 截至第 176 帧累计失败 0；IMU 初始化后连续工作 | 当前短时运行，不代表任意长轨迹或掉线测试 |

## 实现

- 已通过质量检查的视觉约束最多等待 3 秒，让延迟到达的雷达位姿参与同一时刻融合。
  等待上限与建图等待预算一致，雷达结果到达便立即继续。
  雷达不可用时释放视觉约束；队列有限长，异常视觉会清空待发约束。
- 104 只发布有测量支持的 EKF 时间状态。超时产生的纯外推不能抢占时间戳，
  阻止后续较早的正确测量发布。保留相同时间戳的后续修正。
- 视觉接管后，较早到达且与同刻可靠雷达一致的 EKF 结果可以作为恢复依据。
  该较早位姿不对外发布；后续 EKF 时间追上后恢复融合输出，保持位姿和 TF 时间单调。
  雷达处理时延使用建图的时间预算，不再直接套用图像的 1.5 秒预算。
- EKF 的 `predict_to_current_time=false` 本身不能禁止传感器超时外推，
  这一行为可见 [robot_localization 的 integrateMeasurements](https://github.com/cra-ros-pkg/robot_localization/blob/humble-devel/src/ros_filter.cpp)。
- `hardware.split_inertial: true` 将 IMU、LiDAR、双目分为三个接收进程。
  IMU 进程估计并冻结时钟；LiDAR 读取同一运行目录中的原子状态文件，使用完全相同的时间偏移。
  共享状态过期、时钟复位或偏移变化时拒绝使用，不能各自重新拟合时钟。
- 高程标准差上限仍为 0.25 m，没有通过降低质量要求强行填图。
- 外发局部图仍为 64 m，237 右侧仍为 32 m 跟车视野；局部周期 1 秒，全局 2 秒。

## 验证与记录

- 4 项约束排序单测、14 项高程融合单测、8 项硬件时钟单测通过。
- 实际 ROS EKF 延迟回归，以及位姿／TF 跳变拒绝与延迟测量恢复回归通过。
- 较早 EKF 恢复、错误旧位姿拒绝、后续雷达失效视觉接管测试通过。
- 黑屏、过曝、雷达失效、融合器发散、双源同时失效和位姿／TF 一致性回归通过。
- Python ROS 包编译成功，源码／运行安装一致性检查通过。
- 已验证本轮在线地图增长。此前连续失配时视觉继续输出但高程置信度不足的记录仍保留，
  更长轨迹精度及真实断流后的恢复仍需后续验证。
- 当前在线地图显示约有 2–3 秒时延，仍受雷达处理、融合等待和发布周期共同影响。

104 上的原始记录：

- `results/delayed_height_order_20260917_v2/report.json`
- `results/run_20260917_161939/imu_timing_probe.json`
- `results/run_20260917_161939/imu_timing_split_probe.json`
- `results/split_bridge_probe_20260917_163146/`
- `results/run_20260917_161939/height_timing_live_check.json`
- `results/run_20260917_165024/height_timing_live_check.json`
- `results/run_20260917_165024/height_timing_live_check_steady.json`

IMU 已初始化，可以继续手动行驶测试。本次重启已更换地图原点，P4 需要重新对齐。
