> **阶段记录**：本文保留当时的实现与测量。当前默认配置、修复结果和多轨迹对照以 [最终多轨迹报告](fusion_multitrajectory_report.md) 为准。

# short bag 基线测试

数据：`/home/yanfa/P3/roma_t3_algorithm_bundle_20260825/recordings/short_20260915_190151/bag`。

基线输出：`results/short_xfeat_reliable_20260915_211742/assessment.json`。这份报告描述旧的 LiDAR 优先门控，不能作为新自适应分支的成绩。

## 数据条件

- 58 组同步双目＋LiDAR，传感器时间跨度 232.407 秒，正常时间戳原速回放。
- 采集间隔中位数 4.047 秒，约 0.245 Hz。它限制观测更新率；不能把它当成算法处理耗时。
- 没有 IMU，用户已确认当前仿真不提供 IMU；点云无逐点时间，按瞬时仿真点云处理。
- 参考为 `/car/pose`，只用于事后评估，从未输入定位。按原 P3 实现做固定尺度 SE(3) 对齐，不拟合尺度或时间偏移。
- 参考与曝光的绝对时间同步尚未认证；轨迹几乎平面，因此成绩是该段位置诊断，不是正式实机精度认证。

## 已完成基线的结果

| 项目 | 结果 |
|---|---:|
| 独立视觉位置 RMSE | 0.09876 m |
| 无 IMU Point-LIO 位置 RMSE | 2.56892 m |
| 最终融合位置 RMSE | 2.56872 m |
| 最终融合位置误差 P95 | 3.78595 m |
| 视觉稳定处理时间中位数 / P95 | 225.7 / 302.0 ms |
| 输入发布至 LiDAR 门控接收中位数 / P95 | 439.1 / 460.4 ms |
| 完整本任务进程树 RSS 峰值，含 RViz | 3028.2 MiB（2.96 GiB） |

LiDAR 接收延迟含传输和适配，不是配准内核计时。视觉处理时间只统计正常跟踪帧；首帧冷启动约 1.084 秒，因总年龄超过 1.5 秒被拒绝，单独保留在全尝试统计中。

**基线融合不合格：**门控仅发布过一个高协方差视觉锚点，没有可用的双帧差分视觉区间。不能把独立视觉约 10 cm 的误差写成融合精度。原始话题存在、状态显示融合，均不足以证明视觉发挥作用。

58/58 组图像完整进入前端；高程图、点云图、位姿、轨迹、增量接口及唯一主 TF 等 9 项检查通过。接口通过不代表轨迹精度通过。

建图 56 次，持久化 476036 个点云体素；显示最多 150000 点；32 个高程块占数组约 30.5 MiB。停止后已导出 PCD 和原项目可读取的无损快照。无地表参考，未评估高程误差。

## 修正与后续结果

新分支根据来源独立质量选择约束，不再让退化 LiDAR 一票否决视觉。真实回放前的 ROS 合成切换测试覆盖：退化 LiDAR＋健康视觉、黑屏＋健康 LiDAR、视觉以不同原点恢复、两路同时无效。最大连续位姿步长 0.02 m，符合合成输入；两路无效时没有新合格输出。这只是状态机回归，不是实景精度。

新分支见 [无 IMU 候选与切换说明](candidate_status.md)。实际 VoxelMap 回放成绩以其独立运行目录中的 `assessment.json` 为准。
