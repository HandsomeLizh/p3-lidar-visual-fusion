# 104：双目几何建图，视觉/LiDAR/IMU 融合定位

## 分工

- 定位代码及其参数保持原样。地图使用 `/T3/semantic/current_pose` 正式融合位姿。
- `mapping_source: stereo` 时，建图端只订阅 `/fusion/stereo_points`，不订阅雷达或 ToF 点，也会拒绝通过其他入口送来的测距点。
- 通过双目、时序匹配及位姿质量检查的三维点，转换到地图坐标后逐帧累积；使用已有磁盘体素存储及有限预览，不另加深度网络。
- 三维地图 `/T3/mapping/stereo_map` 保留真实三维观测，不把栅格中心或拟合地面点伪装成双目点。此模式不发布 `/T3/mapping/lidar_map`。
- 局部/全局高程话题保持不变，全部来自双目观测，地面参考也由双目点拟合。未知格保留为 NaN。三维云可保留高处表面，高程只融合符合近地表条件、连续确认过的观测。
- 视觉不合格、位姿质量不合格时暂停入图；LiDAR 仍可维持定位，但不会用雷达点补地图。

配置仍是每秒最多 2 帧、每帧最多 512 个双目点；双目属于稀疏特征点云。磁盘体素 0.12 m、预览体素 0.25 m、预览最多 15 万点。全局预览有上限，完整体素图保存在运行目录中。自车范围过滤继续生效。

## 验证

候选目录：`/home/yanfa/P3/lidar_visual_fusion_hw_stereo_map_20260917`。

- 独立 ROS 检查通过：实际三维坐标保存、随位姿累积、纯双目高程、车体过滤、坏质量暂停、LiDAR/ToF 排除、旧地图来源不兼容时拒绝续写。
- 已有真实停车图像：9 个有效双目对，合格点每帧 151～163，累积 322 个三维体素，产生 372 次高程格更新（不是 372 个不同格）。雷达/ToF 入图均为 0。使用固定试验位姿与 CPU 前端，不是实时速度或行驶精度测试。
- 48 项回归通过；原雷达地图与混合补图模式仍有独立测试。

离线验证结果在候选目录 `results/hardware104_stereo_only_20260917/`。

### 已部署与在线检查（2026-09-17）

用户确认后已停止旧运行，部署到 `/home/yanfa/P3/lidar_visual_fusion` 并重新启动建图及组合窗口。当前新运行目录为 `results/run_20260917_123448`，ROS_DOMAIN_ID 为 59。237 未启动显示。

30 秒被动检查通过，没有发送车辆运动指令：

- 正式位姿约 3.70 Hz，视觉约 3.55 Hz，LiDAR 约 2.70 Hz；真实 IMU 已初始化并参与 LiDAR 定位。
- 双目观测约 1.55 Hz；累积点云和局部高程约 0.93 Hz，全局高程约 0.50 Hz。
- 双目入图从 266 帧增至 309 帧；地图体素从 394 增至 401，局部和全局图均有 104 个已观测高程格。静止重复观测不会持续增加大量新体素。
- `lidar_scans=0`、`tof_scans=0`，`/T3/mapping/lidar_map` 发布者为 0。检查的全部话题均无时间戳倒退。
- 这段静止检查的位姿最大单步变化约 2.3 mm；仅说明本次短时静止表现，不代表行驶精度。日志有少量无有效扫描时长的原始雷达帧被拒绝，有效 LiDAR 定位继续输出。

部署及在线证据在正式目录的 `results/hardware104_stereo_only_20260917/deployment.json` 和 `live_switch.json`。当前已换新地图原点，P4 使用前需要重新对齐。

## 切换与显示

切换需要新的运行目录和地图原点；停止旧运行后部署新版，再通过原启动脚本开启。P4 需要随新地图重新对齐。重新打开显示窗口，避免保留旧雷达地图的显示缓存。

现有组合窗口已订阅 `/T3/mapping/stereo_map`，可直接显示新的累积三维点云。对于未配置过且系统不确定的新电脑，最通用的是用 RDP 远程桌面连接 `192.168.100.104:3389`，在 104 的桌面内运行带 `--rviz` 的启动脚本；新电脑无需安装 ROS、模型或点云处理程序。

若要求在新电脑本地运行 RViz，则需要知道其系统后安装 ROS/Qt/显示消息依赖；原 237 的显示目录依赖 237 已有 ROS 环境，不能直接视作任意系统的免安装包。
