# 104 车机部署验证记录

日期：2026-09-16。主机：`yanfa@192.168.100.104`。
目录：`/home/yanfa/P3/lidar_visual_fusion`；分支：`hardware104`。

## 交付范围

- 同步 P3 融合模块、私有依赖和离线视觉权重；完成 ARM64 / ROS 2 Humble 编译。
- 真实 Ouster OS1 点云、其内置 IMU、Galaxy 双目接入；不使用仿真 capture。
- 沿用用户确认未改动的 8 月 17 日安装标定，记录来源文件及哈希。
- 双目在私有驱动内全视野缩小至 640×536 mono8，再进入 ROS 和算法；同步缩放内参。
- 雷达与 IMU 共享固定时间偏移，原始旋转扫描补偿到扫描末端。
- 合入 216 的视觉接续与无 IMU 子地图恢复；IMU 正常时仍使用惯性预测，不由视觉覆盖雷达初值。
- 保留组合 RViz、障碍显示、P4 目标与路线接口、局部/全局地图及保存功能。

测试没有发布车辆速度或导航目标；用户要求保留原有 GLIM，不为本模块释放其资源。

## 已通过的检查

| 检查 | 结果与范围 |
|---|---|
| 最终算法编译 | `8178163`，6 个 ROS 包通过；相机驱动、组合窗口另已编译 |
| Python 回归 | 时间、双目几何、核心逻辑、视觉连续性共 20 项通过 |
| IMU 扫描补偿 | 合成旋转/平移数值误差约 2.54×10⁻⁷ 米 |
| IMU 断流故障注入 | 缺失区间不输出未补偿扫描，连续数据恢复后重新配准；无视觉初值覆盖 |
| 视觉接续 ROS 检查 | 150 帧 LiDAR 失败期间，150 帧合格视觉输出；黑屏和无参考视觉重置被拒绝 |
| 无 IMU 子地图恢复 | 编译后端通过旧图恢复、候选确认、失败回滚及弱几何拒绝 |
| 组合管线恢复 | 24 帧配准失败期间持续发布视觉位姿并更新地图；后续子地图恢复 |
| 启停与窗口 | `operator_03`：两侧视图、启动状态、重复启动检查、保存与停止流程通过；本模块及窗口峰值约 2751 MiB，不含独立传感器驱动 |

后三项恢复测试使用隔离 ROS 域、合成几何和前端消息，验证故障处理逻辑，不能代替实车运动精度测试。

## 真实传感器结果

### 较早完整静态检查：`results/hardware104_static_04`

该轮在视觉接续合并之前运行 65 秒。真实双目、LiDAR、IMU 同时工作，175/175 帧 LiDAR 配准有效，视觉约束接收并接受 163 次，发布局部和全局地图。LiDAR 单帧耗时中位数 0.329 秒；驻车首尾位移 0.00211 米。

### 最新检查：`results/hardware104_static_05`

算法为 `8178163`，配置启用合并后的视觉接续。运行 65 秒：

| 项目 | 实测 |
|---|---|
| 有效 LiDAR 配准 | 171/171 帧，全部使用 IMU |
| LiDAR 后端单帧耗时中位数 | 0.318 秒 |
| 驻车首尾位移 | 0.00167 米 |
| 输入 | 左右图各 307 张、IMU 5964 条 |
| 正式位姿 / 局部地图消息 | 173 / 16 条 |
| 结束时距最新补偿点云 / 位姿接收 | 0.093 / 0.352 秒 |
| 时钟参考预热抖动 | 0.00226 秒；桥接未报告 IMU 过期或时钟漂移 |
| 实际视觉位姿 | 0 条，模型初始化未完成 |
| 全局栅格消息 | 因系统可用内存约 1 GiB，进入内存保护而暂停；局部地图仍更新 |

**因此本轮只认定雷达＋IMU＋局部建图通过，完整三传感器联调尚未通过。** `static_05/report.json` 中原来的 `passed` 只检查了 LiDAR/IMU 和地图，不能据此认定视觉通过；后续测试脚本已分开记录两项并要求同时通过。

该轮系统内存 61 GiB，原有 GLIM 约占 45～47 GiB，交换区使用约 23～26 GiB。视觉测试进程在内核不可中断等待（D 状态），已发送停止信号，无法保证立即退出；原有 GLIM 未停止、系统未重启。恢复资源前不应反复启动新的视觉实例。`start_hardware.sh` 增加了 4 GiB 可用内存的启动检查。

## 当前限制

- 上述首尾位移是驻车观察，**不是 ATE**，不证明行驶时不会跳变。尚未进行人工直行、转弯及独立真值精度检查。
- 软件时间偏移不等同于硬件同步；动态外参方向与时间配合仍待联合核验。
- 带真实 IMU 的候选子地图重建未单独扩展验证；当前 raw scan 建图需要完整 IMU 补偿。
- ToF 暂未入图；动态旧障碍清理在实机配置中暂时关闭，待真实噪声和运动验证。
- 视觉不能提供合格位姿时不参与融合；纯双目深度后备建图尚未实现。
- P4 沿用接口并使用建图域 59；本次未启动导航或验证真实车辆移动。

## 文件

- 使用说明：`docs/HARDWARE104_CN.md`
- 实机配置：`config/hardware104.yaml`
- 编译、单元及故障注入日志：`results/hardware104_deployment/`
- 静态真实数据报告：`results/hardware104_static_04/report.json`、`results/hardware104_static_05/report.json`
- 窗口与脚本检查：`results/hardware104_operator_03/`
- 各测试报告保留原始记录，未删除失败或未完成的尝试。
