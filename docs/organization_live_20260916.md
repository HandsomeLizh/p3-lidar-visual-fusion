# 目录整理与实时短测记录

日期：2026-09-16。

## 目录整理

范围仅为 `/home/yanfa/P3/lidar_visual_fusion`，没有整理其他 P3/P4 项目。

- 46 个已结束的旧结果目录移入 `results/history/日期/`，共 732,941,799 字节（约 699 MiB）。这是归档，不是释放磁盘。
- 整理时保留 37 个正在使用、最近更新或被报告／测试引用的目录。
- 原始 bag、模型、依赖、构建产物和常用启动脚本位置保留。
- 归档清单：`results/organization/20260916_174947_applied.json`，逐项记录原路径、新路径、文件数和体积。
- 增加 `results/latest`，后续启动时更新为当前结果目录的相对软链接。以 `run_state.json` 为最终依据。
- 增加文档索引，更新使用说明和接口说明中的过时频率、地图层与 TF 描述。

## 实时测试执行到哪里

旧会话地图已保存。停止旧会话后，另一个启动入口创建了 `results/run_20260916_175037`；本次 `start_live.sh` 因检测到已有任务而拒绝重复启动。短测观察的是这套已经运行的管线，不能把启动失败的入口称为成功录制了输入 bag。

17:53:23 向 P4 发送一个约 3.4 米前方的目标。实际观察：

| 项目 | 结果 |
|---|---|
| P4 路径 | 目标发出后约 34 ms 收到 4 点全局路线、约 36 ms 收到 7 点局部路径；这是话题到达时间，不是规划器纯计算耗时 |
| 控制指令 | 线速度始终为 0；角速度最大 0.03 rad/s，仅发生转向 |
| 有限测试时段 | 约 16 秒后因位置反馈接收间隔超过 0.5 秒而中止 |
| 停车 | 发出 `park`；随后反馈保持新鲜，3 秒检查窗位置变化约 0.0066 米 |
| 记录覆盖 | 含起止观察共约 28 秒，32 条正式位姿、9 次局部图、10 次全局图 |
| 观测间隔 | 正式位姿最大约 1.63 秒，局部图最大约 4.24 秒，全局图最大约 4.38 秒；2 秒是配置目标，不是本次每次都达到 |

参考位置累计变化约 0.037 米，包含转向时的车体位置变化，不能作为有效直线路程。**没有计算本次 ATE／总路程，也没有通过新版本在线行驶精度验收。** 观察结束时正式位姿与 P4 对齐反馈仍有明显位置差，需要后续在独占测试时段检查；本段还存在并发使用，不能据此确定是哪一路算法或控制导致。

原始观察和结果在 `results/live_motion_20260916/straight_01/observations.jsonl`、`result.json`。P4 反馈只用于记录和停车监督，不发布给融合器。

随后用户确认有其他人使用仿真车，要求暂不运动。本次不再发控制指令、不再重启运行中的程序。后续待设备空闲后再完成直行、转弯、停车漂移和 LiDAR 失配恢复的实际轨迹验收。
